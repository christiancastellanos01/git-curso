git init = para iniciar un repositorio
git status = para ver el estado del repositorio
git add = para agregar archivos al repositorio
git commit = para hacer un commit, aqui aparece la consola de git para escribir el mensaje del commit
git commit -m "mensaje" = para hacer un commit con un mensaje sin usar la consola
git log = para ver el historial de commits
git diff = para ver las diferencias entre commits
git checkout = para moverse entre
git checkout -- nombre del archivo = para descartar cambios en un archivo
git config --global user.name "Tu Nombre" = para configurar tu nombre de usuario globalmente
git config --global user.email "tuemail@example.com" = para configurar tu correo electrónico globalmente